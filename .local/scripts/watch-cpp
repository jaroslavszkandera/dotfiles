#!/bin/bash


compileFile=''
for i in "$@"; do
	[ -f "$i" ] && compileFile="$i" && break
done

if [ -n "$compileFile" ]; then
	entr -c do-cpp -x "$@" <<<"$compileFile"
else
	echo "Can't find file to keep watching" >&2
	exit 1
fi
