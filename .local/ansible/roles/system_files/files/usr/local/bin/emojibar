#!/bin/sh


chosen="$(cut -d ';' -f1 "$(dirname "$0")/emojis.txt" | rofi -dmenu -i -l 20 | sed "s/ .*//")"
[ -n "$chosen" ] || exit 0

echo "$chosen" | tr -d '\n' | wl-copy
notify-send "'$chosen' copied to clipboard." &
