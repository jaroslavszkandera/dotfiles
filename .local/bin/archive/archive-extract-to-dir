#!/bin/sh


for file in "$@"; do
	(
	NEWDIR="$(basename "${file%.*}")"
	mkdir "$NEWDIR" && cd "$NEWDIR" || exit

	if ! archive-extract-here "$file"; then
		cd ../ && rm -rf "$NEWDIR" || exit
	fi
	) || exit
done
